import 'package:flutter/material.dart';

/// This is the HomeScreen of the app.
/// Right now it is empty, but later you can add your app content here.
class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // Scaffold gives a basic page structure:
      // - appBar
      // - body
      // - floatingActionButton, etc.
      // For now, it's empty, so the screen will be blank.
    );
  }
}
